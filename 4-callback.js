setTimeout(() => {
    console.log('Two seconds are up')
}, 2000)

const name = ['An', 'Jen', 'Jackie']
const shortNames = names.filter((name) => name.length <= 4)

const geocode = (address, callback) => {
    setTimeout(() => {
        const data = {
            latitude: 0,
            longtitude: 0
        }

        return data
    }, 2000)
    
}